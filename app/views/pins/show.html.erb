<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @pin.name %>
</p>

<p>
  <strong>Image:</strong>
  <!-- allows us to see the images we upload -->
  <%= image_tag(@pin.image_url, :width => 200) %>

</p>

<p>
  <strong>User:</strong>
  <%= @pin.user.first_name %>
</p>

<p>
  <strong>Board:</strong>
  <!-- To change the display from reference number to associated name -->
  <%= @pin.board.name %>
</p>

<%= link_to 'Edit', edit_pin_path(@pin) %> |
<%= link_to 'Back', pins_path %>
