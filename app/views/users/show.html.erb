<p id="notice"><%= notice %></p>
	<%= @some_text %>
<p>
<br>	
  <strong>First name:</strong>
  <%= @user.first_name %>
</p>

<p>	
  <strong>Last name:</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p> Showing all of <%= @user.first_name %>'s Boards: </p>
	<ul>
		<!-- all boards associated with speciic user run each do on each instance of boards. board is a bloc variable we can now use in our loop -->
		<% @boards. each do |board| %>
			<li><%= link_to board.name, board_path(board) %>	
			</li>
		<% end%>
	</ul>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
